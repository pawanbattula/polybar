[module/dwm-tags]
type = custom/script
exec = $HOME/.config/polybar/dwm-tags.sh --tail
tail = true

; Format
format = <label>
format-foreground = ${colors.fg}
format-background = ${colors.bg}
format-padding = 1

label = %output%

; Active tag color (currently selected)
env-DWM_TAG_ACTIVE_COLOR = #ECEFF4
; Occupied tag color (has windows) - was bg-alt (#3b4252), nearly invisible on the bar; use cyan for real contrast
env-DWM_TAG_OCCUPIED_COLOR = #5e81ac
; Urgent tag color (urgent hint set)
env-DWM_TAG_URGENT_COLOR = #BF616A
; Empty tag color
env-DWM_TAG_EMPTY_COLOR = #4c566a

; Font customization: (check fonts.ini for available fonts)
; Font index for active tags
env-DWM_TAG_ACTIVE_FONT = 2
; Font index for occupied tags
env-DWM_TAG_OCCUPIED_FONT = 1
; Font index for urgent tags
env-DWM_TAG_URGENT_FONT = 3
; Font index for empty tags
env-DWM_TAG_EMPTY_FONT = 1

; Features:
; - Active tags: Shows currently selected tag (bright)
; - Occupied tags: Shows tags with windows (cyan, high contrast)
; - Urgent tags: Shows tags with urgent windows (red)
; - Empty tags: dim gray, still visible against the bar
; - Works across all monitors